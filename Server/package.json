{
  "name": "server-root",
  "version": "1.0.0",
  "scripts": {
    "start": "node start-all.js",
    "install-all": "cd gateway && npm install && cd ../api-service && npm install && cd ../game-service && npm install"
  },
  "dependencies": {
    "axios": "^1.15.2",
    "sqlite3": "^5.1.0",
    "ws": "^8.20.0"
  }
}
